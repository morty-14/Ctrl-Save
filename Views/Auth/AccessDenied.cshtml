@{
    ViewData["Title"] = "Access Denied";
}

<div class="container text-center" style="padding: 4rem 1rem;">
    <i class="bi bi-shield-x" style="font-size: 4rem; color: var(--cs-green);"></i>
    <h1 class="mt-3">Access Denied</h1>
    <p class="text-muted">You don't have permission to view this page.</p>
    <a asp-controller="Home" asp-action="Index" class="cs-btn mt-2">Go Home</a>
</div>
